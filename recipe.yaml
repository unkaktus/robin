context:
  version: 1.1.0
package:
  name: "spanner"
  version: '{{ version }}'

source:
  path: .

requirements:
  build:
    - go

build:
  script:
    - env CGO_ENABLED=0 go build -o ${PREFIX}/bin/spanner ./cmd/spanner